name: 'job_intelligent'
version: '1.0.0'
config-version: 2

profile: 'job_intelligent'

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  job_intelligent:
    staging:
      +materialized: view
    marts:
      +materialized: table